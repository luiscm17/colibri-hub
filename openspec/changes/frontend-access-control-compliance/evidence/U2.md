# U2 Evidence: Administration Navigation Recovery

## Automated Evidence

| Check | Result |
|---|---|
| RED | `pnpm --dir frontend exec vitest run src/features/access-control/administration/route-state.test.ts` exited 1 because `route-state.ts` did not exist. |
| GREEN | `pnpm --dir frontend exec vitest run src/features/access-control/administration/route-state.test.ts` exited 0: 1 file, 2 tests passed. |
| Build | `pnpm build` exited 0. |
| Lint | First `pnpm lint` run exited 1 on an unused destructuring binding in `route-state.ts`; corrected it. Final `pnpm lint` exited 0. |
| Diff | `git diff --check` exited 0. |

## Scope and Rollback

`route-state.ts` captures a serializable administration origin, preserves a declined dirty draft, restores and clears a confirmed draft, and recovers stale detail or empty collection pages without putting drafts in URLs or storage. `AdministrationShell.tsx` owns this local route/origin state.

Revert `route-state.ts`, `route-state.test.ts`, `AdministrationShell.tsx`, the `AdministrationPage.tsx` shell composition, U2 task checkboxes, and this evidence file to remove U2 without affecting U1 or later units.

## Manual Deferral

Backend/manual S3-S4 navigation scenarios are explicitly deferred to final PR9 under maintainer policy. They were not run and are not claimed as passed in this intermediate work unit.

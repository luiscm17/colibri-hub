# U1 Evidence: Administration Route Matrix

## Automated Gate

- Focused unit test: `pnpm --dir frontend exec vitest run src/features/access-control/administration/operations.test.ts` (2/2 passed).
- Frontend build: `pnpm --dir frontend build` (passed).
- Frontend lint: `pnpm --dir frontend lint` (passed).
- Diff validation: `git diff --check` (passed).

These automated checks gate U1 and its intermediate delivery. All manual/browser scenarios are deferred, non-gating, to final PR9.

## Historical Manual Observation

The prior manual run remains factual evidence, not U1 acceptance:

- S1: authenticated refresh at `/access/roles` failed at that time by rendering `Unsupported administration state` and issuing no roles collection request.
- S2: `/access/scopes/scope-does-not-exist` passed by reaching the application 404 without issuing a scope-detail request.

PR9 must rerun S1 and S2 with the complete chain. No browser or manual scenario was run for this corrective work unit.

## Rollback Boundary

Revert the U1 operation matrix, explicit route/page wiring, focused test, task checkboxes, and this evidence file without removing U2-U9 work.

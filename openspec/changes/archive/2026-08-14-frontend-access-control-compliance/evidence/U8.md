# U8 Evidence: Safe Replacement Confirmation

## Automated Evidence

| Check | Result |
|---|---|
| RED | `pnpm --dir frontend exec vitest run src/features/access-control/administration/mutations/accessibility.test.tsx` exited 1 before implementation because the test environment lacked `window.matchMedia`; the test was configured with the project's Mantine mock before production changes. |
| GREEN | Same command exited 0: 1 file, 1 test passed. |
| Access suite | `pnpm --dir frontend exec vitest run src/features/access-control` exited 0: 12 files, 33 tests passed. |
| Build | `pnpm --dir frontend build` exited 0. |
| Lint | `pnpm --dir frontend lint` exited 0. |
| Diff | `git diff --check` exited 0. |

## Work Unit Evidence

| Evidence | Exact result |
|---|---|
| Focused test | `pnpm --dir frontend exec vitest run src/features/access-control/administration/mutations/accessibility.test.tsx` exited 0: 1 file, 1 test passed. |
| Runtime harness | N/A: manual/browser scenarios, including keyboard, narrow-layout, and assistive-technology checks, are deferred to PR9 by maintainer instruction and were neither run nor claimed passed. |
| Rollback boundary | Revert the confirmation state, modal, and dialog wiring in `UserRoleReplacementPanel.tsx` and `SharedRolePermissionPanel.tsx`, `accessibility.test.tsx`, U8 task marks, and this evidence file; U1-U7 remain intact. |

## Scope and Architecture

Each existing specialized gate remains its own authority. Its existing ready preview only opens a Mantine confirmation dialog; cancellation changes no gate state, while confirm invokes the existing apply-once request. The dialog labels backend-computed impact as proposed-change impact, announces it through `role="status"`, blocks outside-click closure, and uses Mantine focus return. Editing or apply failure clears confirmation together with the existing preview invalidation.

`AdministrationPage` remains the canonical detail owner and its existing panel bindings are unchanged. App composition remains through `administration-route.tsx`; no shared extraction, generic `MutationGate`, parallel state, or orphaned production module was introduced. The focused test directly consumes the user panel; the shared-role panel is directly consumed by `AdministrationPage`.

## Manual Deferral

All manual/browser scenarios are deferred to final PR9. No browser, keyboard, responsive, screen-reader, or real-backend runtime scenario was run or claimed passed for U8.

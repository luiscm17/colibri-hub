# U3 Evidence: Permission Selection Matrix

## Automated Evidence

| Check | Result |
|---|---|
| RED | `pnpm --dir frontend exec vitest run src/features/access-control/administration/forms/matrix.test.ts` exited 1 because `forms/matrix.ts` did not exist. |
| GREEN | `pnpm --dir frontend exec vitest run src/features/access-control/administration/forms/matrix.test.ts` exited 0: 1 file, 5 tests passed. |
| Access suite | `pnpm --dir frontend exec vitest run src/features/access-control` exited 0: 7 files, 24 tests passed. |
| Build | `pnpm --dir frontend build` exited 0. |
| Lint | `pnpm --dir frontend lint` exited 0. |
| Diff | `git diff --check` exited 0. |

## Scope and Rollback

`forms/matrix.ts` is Access-capability-private policy projection for form behavior. It derives effective permissions from active role IDs, permits only exact active scope/action catalog pairs, and marks inactive historical references removable rather than selectable. It does not authorize operations, alter `ACCESS_CATALOG`, make requests, or expose an application contract.

Revert `forms/matrix.ts`, `forms/matrix.test.ts`, the U3 task checkboxes, and this evidence file to remove U3 without changing U1/U2 routing/recovery or later governance work.

## Architecture and Orphan Proof

The matrix remains under `features/access-control/administration/forms/`; no shared extraction or app import was introduced. Application composition still crosses the narrow `administration-route.tsx` entry only. No `GovernancePanel` or `MutationGate` reference was introduced, and U3 adds no orphaned production module: `matrix.ts` is covered by its colocated focused test.

## Manual Deferral

Backend/manual/browser S5 and all manual/browser acceptance remain explicitly deferred to final PR9 under maintainer policy. They were not run and are not claimed as passed in this intermediate work unit.

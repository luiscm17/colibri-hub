# U4 Evidence: Independent Preset Flows

## Automated Evidence

| Check | Result |
|---|---|
| RED | `pnpm --dir frontend exec vitest run src/features/access-control/administration/presets/presets.test.ts` exited 1 because `presets/presets.tsx` did not exist. |
| GREEN | `pnpm --dir frontend exec vitest run src/features/access-control/administration/presets/presets.test.ts` exited 0: 1 file, 2 tests passed. |
| Access suite | `pnpm --dir frontend exec vitest run src/features/access-control` exited 0: 8 files, 26 tests passed. |
| Build | `pnpm --dir frontend build` exited 0. |
| Lint | `pnpm --dir frontend lint` exited 0. |
| Diff | `git diff --check` exited 0. |

## Scope and Rollback

Exact copy sends only the existing `POST /access/role-presets/{presetId}/roles` contract. Adjustable copy takes a deep snapshot into a separate role draft; neither flow holds a live preset reference, so later preset changes do not synchronize.

Revert `presets/`, the preset-detail panel in `AdministrationPage.tsx`, U4 task checkboxes, and this evidence file to remove U4 without changing U1-U3 routing, recovery, or permission-matrix behavior.

## Architecture and Orphan Proof

All U4 code remains capability-private under `features/access-control/administration/presets/`. `AdministrationPage` remains the read/render owner and consumes the panel directly; application composition still imports only `administration-route.tsx`. The production panel consumes the helpers and the focused test covers both flows. No shared extraction, `GovernancePanel`, or `MutationGate` reference was introduced.

## Manual Deferral

Backend/manual/browser S6 and all manual/browser acceptance remain explicitly deferred to final PR9 under maintainer policy. They were not run and are not claimed as passed in this intermediate work unit.

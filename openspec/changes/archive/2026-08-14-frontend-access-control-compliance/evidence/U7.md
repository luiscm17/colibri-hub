# U7 Evidence: Shared Role Permission Gate

## Automated Evidence

| Check | Result |
|---|---|
| RED | `pnpm --dir frontend exec vitest run src/features/access-control/administration/mutations/shared-role-gate.test.ts` exited 1 because `shared-role-gate.ts` did not exist. |
| GREEN | Same command exited 0: 1 file, 2 tests passed. |
| Access suite | `pnpm --dir frontend exec vitest run src/features/access-control` exited 0: 11 files, 32 tests passed. |
| Build | `pnpm --dir frontend build` exited 0. |
| Lint | `pnpm --dir frontend lint` exited 0. |
| Diff | `git diff --check` exited 0. |

## Scope and Rollback

The Access-private shared-role gate normalizes permission pairs, blocks a zero-delta preview, binds ready state to role/version/authority/request generation, uses the preview version and optional reason for one apply request, and invalidates on edits, authority replacement, stale responses, and failed apply. The panel labels backend impact separately as users affected by the proposed change; role metadata remains local input to the supported full-replacement endpoint.

Revert `administration/mutations/shared-role-gate.ts`, `administration/mutations/SharedRolePermissionPanel.tsx`, `administration/mutations/shared-role-gate.test.ts`, the U7 panel import/rendering in `AdministrationPage.tsx`, U7 task checkboxes, and this evidence file. U1-U6 remain intact.

## Architecture and Orphan Proof

`SharedRolePermissionGate` is private to Access administration mutations, has direct production use from `SharedRolePermissionPanel`, and has a direct focused test consumer. `AdministrationPage` remains the canonical role-detail render owner; app composition remains only through `administration-route.tsx`. No generic `MutationGate`, compatibility wrapper, shared extraction, duplicate U6 state, `GovernancePanel`, or `governance` module was introduced.

## Manual Deferral

Backend/manual/browser S8 and every manual/browser scenario remain deferred to final PR9 by maintainer policy. They were not run and are not claimed as passed for U7.

# U5 Evidence: Scopes and History

## Automated Evidence

| Check | Result |
|---|---|
| RED | `pnpm --dir frontend exec vitest run src/features/access-control/administration/scopes/history.test.ts` exited 1 because the test file did not exist; after adding it, it exited 1 because `scopes/history.ts` did not exist. |
| GREEN | `pnpm --dir frontend exec vitest run src/features/access-control/administration/scopes/history.test.ts` exited 0: 1 file, 2 tests passed. |
| Access suite | `pnpm --dir frontend exec vitest run src/features/access-control` exited 0: 9 files, 28 tests passed. |
| Build | `pnpm --dir frontend build` exited 0. |
| Lint | `pnpm --dir frontend lint` exited 0. |
| Diff | `git diff --check` exited 0. |

## Scope and Rollback

Scopes expose only registration of unregistered recognized definitions and collection-version lifecycle requests. They grant no permissions. History keeps its four URL filters and renders only returned actor, time, reason, subject, and change kind; it does not request or fabricate detail or before/after values.

Revert `administration/scopes/`, the U5 sections in `AdministrationPage.tsx`, U5 task checkboxes, and this evidence file. U1-U4 routes, recovery, matrix, and preset flows remain unchanged.

## Architecture and Orphan Proof

U5 remains capability-private under `features/access-control/administration/scopes/`. `AdministrationPage` remains the canonical read/render owner and directly consumes both production components; its colocalized focused test consumes the pure request/read-model helpers. App composition still imports only `administration-route.tsx`, and no shared extraction, new policy authority, `GovernancePanel`, or `MutationGate` reference was introduced.

## Manual Deferral

Backend/manual/browser S6 and every manual/browser scenario remain deferred to final PR9 by maintainer policy. They were not run and are not claimed as passed for U5.

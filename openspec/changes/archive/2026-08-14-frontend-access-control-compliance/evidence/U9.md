# U9 Evidence: Closure Ledger and Maintainer-Confirmed Manual Acceptance

## Automated Evidence

| Check | Result |
|---|---|
| RED | `pnpm --dir frontend exec vitest run src/features/access-control/administration/evidence-completeness.test.ts` exited 1 before this ledger existed because `evidence/U9.md` was absent. |
| Focused GREEN | `pnpm --dir frontend exec vitest run src/features/access-control/administration/evidence-completeness.test.ts` exited 0: 1 file, 1 test passed. The test proves the executable composition/retired-authority ledger; receipt presence is inspected through this evidence document because Vitest source-root policy deliberately denies importing OpenSpec artifacts. |
| Full frontend suite | `pnpm --dir frontend exec vitest run` exited 0: 16 files, 40 tests passed. |
| Full Access suite | `pnpm --dir frontend exec vitest run src/features/access-control` exited 0: 13 files, 34 tests passed. |
| Build | `pnpm --dir frontend build` exited 0. |
| Lint | `pnpm --dir frontend lint` exited 0. |
| Diff | `git diff --check` exited 0. |

## Architecture and Orphan Ledger

All U1-U9 receipts exist and were inspected. `evidence-completeness.test.ts` verifies the absence of retired `MutationGate`, `GovernancePanel`, and `governance` authority names within Access source. Static import proof confirms app routes lazily import only the Access-owned `administration-route.tsx`; no Access source imports the app shell. The test is an Access-private ledger and introduces no production module or consumer. `AdministrationPage` remains the canonical Access detail/render owner, while `administration-route.tsx` remains the only Access entry imported by app routing. The U6 and U7 specialized gates retain their separate direct panel bindings.

## Maintainer-Confirmed Manual Acceptance

The maintainer confirmed the temporary S1-S10 checklist as complete: every prerequisite was checked, every scenario has exactly one **PASS**, and S10 confirmed reconciliation and rollback. This section transfers that human evidence without claiming that an AI ran the browser or real-backend scenarios.

| Scenario | Requirement / receipt mapping | Maintainer-confirmed bounded evidence | Result |
|---|---|---|---|
| S1 | Direct entry; U1 | Supported collections and supported details reconstructed after direct entry and refresh; reads only, no mutation, including the historical Roles-route regression. | PASS |
| S2 | Prohibited state; U1 | Unsupported Scope/History details and Access user creation did not publish a protected view or emit protected/speculative requests. | PASS |
| S3 | Origin and discard; U2 | The intended dirty-discard journey was unavailable because no connected form or paginated fixture exists; maintainer accepted this documented out-of-scope UI limitation as PASS. | PASS |
| S4 | Recovery; U2, U7 | Stale detail recovery, single empty-page fallback, latest-only/abort behavior, and access-loss recovery were observed without retry loops or replay. Draft clearing was unavailable where the required form is unavailable. | PASS |
| S5 | Matrix; U3 | Supported active permission pairs produced the expected preview behavior; unsupported/reserved input did not grant access. Inactive-scope coverage was unavailable because test data had none and was accepted as out of scope. | PASS |
| S6 | Preset independence and governance/History; U4, U5 | Exact copy made one request; adjustable draft did not copy; recognized scope registration and the four History filters worked. Scope lifecycle rollback completed after a fresh read between status changes. | PASS |
| S7 | Fresh replacement; U6 | Zero-delta was blocked without preview; a real replacement made one preview and one apply, confirmation cancellation restored focus, replay was blocked, and the original role set was restored. | PASS |
| S8 | Invalidated replacement; U7 | Shared-role zero-delta, preview, confirm/cancel, conflict invalidation, access-loss handling, no replay, optional empty reason, and restoration were checked. A fresh reload was needed after conflict before a new preview. | PASS |
| S9 | Accessible safe delivery; U8 | Keyboard dialog operation, focus return, live-status DOM semantics, narrow-layout visibility, textual status cues, privacy boundaries, and sign-out clearing were checked. | PASS |
| S10 | Evidence; U9 | S1-S9 states and bounded request/behavior records were reconciled with U1-U9; reversible state was restored by fresh reads and no residual draft or preview remained. | PASS |

### Request, Behavior, and Rollback Boundaries

- Manual request evidence was limited to expected reads and the relevant preview/apply or lifecycle operation. Zero-delta and prohibited-state paths emitted no protected mutation or speculative request; confirmed replacement paths emitted one preview and one apply, with no replay.
- S10 rollback reconciliation confirmed the tested scope, user-role assignment, shared-role permissions, preset state, and temporary preset-copy role were restored or retired through the authorized lifecycle procedure. No residual browser draft, preview, or authenticated protected state was retained.
- S3/S4 form-dependent checks and S5 inactive-scope coverage were not executable in the available UI/data. The maintainer explicitly accepted these documented out-of-scope limitations as PASS; they are not evidence that those unavailable controls exist.

### Non-Blocking Observations and Caveats

- Consecutive scope status changes require a fresh read between changes because the row version advances. A back-to-back activate after deactivate can correctly receive a concurrency conflict from the stale version; the confirmed rollback used a fresh read before the second change.
- Screen-reader-related semantics were verified at the DOM level (`aria-live` / status semantics) without a physical screen-reader pass. The maintainer accepted this as PASS; it is not evidence of physical-reader validation.
- The user-role and shared-role panels may require a refresh/reload before a subsequent change after apply or conflict. This was recorded as a non-blocking UX observation; no automatic replay occurred.

## Rollback Boundary

Revert `evidence-completeness.test.ts`, this `evidence/U9.md` transfer, and the corresponding U9 task mark. This removes only the closure ledger and durable acceptance transfer; U1-U8 implementation and receipts remain intact.

## Bounded Remediation Evidence (2026-08-14)

The two source-level findings in the failed verification were corrected without changing the historical maintainer-confirmed S1-S10 record or this change's completed task checkboxes. `UserRoleReplacementGate` now rejects a second preview while any preview is pending, preserving its existing normalized key, generation, stale-response, invalidation, and apply-once protections. Both specialized panels now retain backend `affected_users`, render the first six names/codes alongside the separate impact count, and expose the remainder through an `aria-expanded` control.

| Check | Result |
|---|---|
| Focused mutation regressions | `pnpm --dir frontend exec vitest run src/features/access-control/administration/mutations/user-role-gate.test.ts src/features/access-control/administration/mutations/shared-role-gate.test.ts src/features/access-control/administration/mutations/accessibility.test.tsx` exited 0: 3 files, 8 tests passed. |
| Full Access suite | `pnpm --dir frontend exec vitest run src/features/access-control` exited 0: 13 files, 37 tests passed. |
| Full frontend Vitest | `pnpm --dir frontend exec vitest run` exited 0: 16 files, 43 tests passed. |
| Build / lint / diff | `pnpm --dir frontend build`, `pnpm --dir frontend lint`, and `git diff --check` each exited 0. The existing >500 kB Vite advisory remained. |

No browser or manual test was run for this source correction. Re-verification must update `verify-report.md`; this remediation record does not change its failed verdict.

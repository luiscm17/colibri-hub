[project]
name = "colibri-hub"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
    "pyjwt>=2.13.0",
    "sqlalchemy>=2.0.51",
]

[tool.uv.workspace]
members = [
    "backend",
]

[tool.fastapi]
entrypoint = "backend.main:app"
